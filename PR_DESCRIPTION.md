# Sync root copies with src

This branch syncs `App.tsx`, `services/geminiService.ts`, and `lib/supabaseClient.ts` in the repository root with their `src/` equivalents, and adds `.github/copilot-instructions.md`.
